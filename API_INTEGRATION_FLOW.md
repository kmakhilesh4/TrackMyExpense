# Complete API Integration Flow

## ✅ YES - Full API Integration is Included!

Both dialogs are fully integrated with the backend APIs. Here's the complete flow:

---

## 🔄 Add Account Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. USER ACTION                                                  │
│    User clicks "Add Account" button in Accounts.tsx            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. DIALOG OPENS                                                 │
│    AddAccountDialog.tsx renders with form                       │
│    - Account Name input                                         │
│    - Account Type selector                                      │
│    - Initial Balance input                                      │
│    - Currency selector                                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. FORM SUBMISSION                                              │
│    handleSubmit() calls:                                        │
│    createAccount.mutateAsync({                                  │
│      accountName: "Main Checking",                              │
│      accountType: "checking",                                   │
│      balance: 1000,                                             │
│      currency: "INR",                                           │
│      isActive: true                                             │
│    })                                                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. REACT QUERY HOOK (useAccounts.ts)                           │
│    createAccount mutation executes:                             │
│    - mutationFn: apiClient.post('/accounts', data)             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. API CLIENT (api.ts)                                          │
│    Axios interceptor adds JWT token:                            │
│    - Fetches token from AWS Cognito                             │
│    - Adds Authorization: Bearer <token>                         │
│    - POST https://api.example.com/accounts                      │
│    - Body: { accountName, accountType, balance, currency }     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. AWS API GATEWAY                                              │
│    Routes request to Lambda function                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. LAMBDA HANDLER (account.ts)                                  │
│    export const create = compose(                               │
│      withErrorHandler,                                          │
│      withAuth,           // ← Validates JWT token               │
│      withValidation      // ← Validates request body            │
│    )                                                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 8. VALIDATION (account.validation.ts)                           │
│    Zod schema validates:                                        │
│    - accountName: required string                               │
│    - accountType: enum [checking, savings, ...]                 │
│    - balance: number (default 0)                                │
│    - currency: 3-letter code (default INR)                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 9. SERVICE LAYER (account.service.ts)                           │
│    accountService.createAccount(userId, data)                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 10. REPOSITORY (account.repository.ts)                          │
│     accountRepository.create(userId, data)                      │
│     - Generates UUID for accountId                              │
│     - Creates DynamoDB item:                                    │
│       UserId: "USER#abc123"                                     │
│       EntityType: "ACCOUNT#uuid"                                │
│       accountName, accountType, balance, currency               │
│       createdAt, updatedAt                                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 11. DYNAMODB                                                    │
│     PutItem operation saves account                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 12. SUCCESS RESPONSE                                            │
│     Returns 201 with created account data                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 13. REACT QUERY CALLBACK (useAccounts.ts)                      │
│     onSuccess:                                                  │
│     - queryClient.invalidateQueries(['accounts'])               │
│     - toast.success('Account created successfully')             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 14. UI UPDATE                                                   │
│     - Dialog closes                                             │
│     - Accounts list automatically refetches                     │
│     - New account appears in the UI                             │
│     - Success toast notification shows                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Add Transaction Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. USER ACTION                                                  │
│    User clicks "Add New" button in Transactions.tsx            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. DIALOG OPENS                                                 │
│    AddTransactionDialog.tsx renders with form                   │
│    - Fetches accounts via useAccounts()                         │
│    - Fetches categories via useCategories()                     │
│    - Type selector (expense/income)                             │
│    - Account dropdown (shows balance)                           │
│    - Category dropdown (filtered by type)                       │
│    - Amount, Description, Date inputs                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. FORM SUBMISSION                                              │
│    handleSubmit() calls:                                        │
│    createTransaction.mutateAsync({                              │
│      accountId: "uuid-123",                                     │
│      categoryId: "uuid-456",                                    │
│      type: "expense",                                           │
│      amount: 50.00,                                             │
│      description: "Grocery shopping",                           │
│      transactionDate: "2024-01-15T00:00:00.000Z"               │
│    })                                                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. REACT QUERY HOOK (useTransactions.ts)                       │
│    createTransaction mutation executes:                         │
│    - mutationFn: apiClient.post('/transactions', data)         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. API CLIENT (api.ts)                                          │
│    Axios interceptor adds JWT token:                            │
│    - POST https://api.example.com/transactions                  │
│    - Authorization: Bearer <token>                              │
│    - Body: { accountId, categoryId, type, amount, ... }        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. AWS API GATEWAY                                              │
│    Routes request to Lambda function                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. LAMBDA HANDLER (transaction.ts)                              │
│    export const create = compose(                               │
│      withErrorHandler,                                          │
│      withAuth,           // ← Validates JWT token               │
│      withValidation      // ← Validates request body            │
│    )                                                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 8. VALIDATION (transaction.validation.ts)                       │
│    Zod schema validates:                                        │
│    - accountId: required string                                 │
│    - categoryId: required string                                │
│    - type: enum [expense, income]                               │
│    - amount: required positive number                           │
│    - description: required string                               │
│    - transactionDate: required ISO date                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 9. SERVICE LAYER (transaction.service.ts)                       │
│    transactionService.createTransaction(userId, data)           │
│    - Verifies account exists                                    │
│    - Prepares transaction creation                              │
│    - Prepares balance update                                    │
│    - Executes ATOMICALLY                                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 10. REPOSITORY (transaction.repository.ts)                      │
│     transactionRepository.prepareCreate(userId, data)           │
│     - Generates UUID for transactionId                          │
│     - Creates transaction object:                               │
│       UserId: "USER#abc123"                                     │
│       EntityType: "TX#2024-01-15#uuid"                          │
│       accountId, categoryId, type, amount, description          │
│       createdAt, updatedAt                                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 11. ACCOUNT REPOSITORY (account.repository.ts)                  │
│     accountRepository.prepareUpdateBalance(userId, accountId)   │
│     - Calculates balance change:                                │
│       * expense: -50.00                                         │
│       * income: +50.00                                          │
│     - Prepares update operation                                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 12. DYNAMODB ATOMIC TRANSACTION                                 │
│     transactWriteItems([                                        │
│       { Put: transaction },      // ← Create transaction        │
│       { Update: account balance } // ← Update balance           │
│     ])                                                          │
│     ⚠️ BOTH succeed or BOTH fail (atomic)                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 13. SUCCESS RESPONSE                                            │
│     Returns 201 with created transaction data                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 14. REACT QUERY CALLBACK (useTransactions.ts)                  │
│     onSuccess:                                                  │
│     - queryClient.invalidateQueries(['transactions'])           │
│     - queryClient.invalidateQueries(['accounts'])  ← Balance!   │
│     - toast.success('Transaction added')                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 15. UI UPDATE                                                   │
│     - Dialog closes                                             │
│     - Transactions list automatically refetches                 │
│     - Accounts list automatically refetches (updated balance!)  │
│     - New transaction appears in the UI                         │
│     - Account balance updates in real-time                      │
│     - Success toast notification shows                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow (Automatic)

```
Every API request includes:

┌─────────────────────────────────────────────────────────────────┐
│ Axios Request Interceptor (api.ts)                             │
│                                                                 │
│ 1. fetchAuthSession() from AWS Amplify                         │
│ 2. Extract JWT access token                                    │
│ 3. Add to headers: Authorization: Bearer <token>               │
│ 4. Send request                                                │
└─────────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ Lambda Middleware (auth.middleware.ts)                          │
│                                                                 │
│ 1. Extract token from Authorization header                     │
│ 2. Verify token with AWS Cognito                               │
│ 3. Extract userId from token claims                            │
│ 4. Pass userId to handler                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 API Endpoints Used

### Account Endpoints
```
✅ GET    /accounts           - List all accounts (used in dialog)
✅ POST   /accounts           - Create account (used in form submit)
✅ PATCH  /accounts/{id}      - Update account (not used yet)
✅ DELETE /accounts/{id}      - Delete account (not used yet)
```

### Transaction Endpoints
```
✅ GET    /transactions       - List transactions (used in page)
✅ POST   /transactions       - Create transaction (used in form submit)
✅ DELETE /transactions?sk=   - Delete transaction (not used yet)
```

### Category Endpoints
```
✅ GET    /categories         - List categories (used in dialog)
✅ POST   /categories         - Create category (not used yet)
✅ PATCH  /categories/{id}    - Update category (not used yet)
✅ DELETE /categories/{id}    - Delete category (not used yet)
```

---

## ✅ What's Included in the Integration

### ✅ Frontend
- [x] React Query hooks for data fetching
- [x] Axios API client with JWT authentication
- [x] Request interceptors for token injection
- [x] Response interceptors for error handling
- [x] Automatic cache invalidation after mutations
- [x] Toast notifications for success/error
- [x] Loading states during API calls
- [x] Error handling and display

### ✅ Backend
- [x] Lambda functions for all endpoints
- [x] JWT authentication middleware
- [x] Request validation with Zod schemas
- [x] Service layer with business logic
- [x] Repository layer for database operations
- [x] Atomic transactions for data consistency
- [x] Error handling middleware
- [x] CORS configuration

### ✅ Data Flow
- [x] Form submission → Hook mutation
- [x] Hook mutation → API client
- [x] API client → AWS API Gateway
- [x] API Gateway → Lambda function
- [x] Lambda → Service → Repository
- [x] Repository → DynamoDB
- [x] Response → Hook callback
- [x] Hook callback → UI update

---

## 🎯 Summary

**YES, FULL API INTEGRATION IS INCLUDED!**

Both dialogs are completely wired up to the backend:
- ✅ Authentication (JWT tokens)
- ✅ API calls (Axios)
- ✅ Data fetching (React Query)
- ✅ Form submission (mutations)
- ✅ Cache invalidation (automatic refresh)
- ✅ Error handling (toast notifications)
- ✅ Loading states (spinners)
- ✅ Atomic operations (transaction + balance)

**You can use these features immediately!** Just start the backend and frontend, and the forms will work end-to-end.
