# AWS Configuration
AWS_REGION=ap-south-1
AWS_ACCOUNT_ID=741846356523

# DynamoDB Configuration
DYNAMODB_TABLE_NAME=TrackMyExpense-prod
# Remove DYNAMODB_ENDPOINT for production (only needed for local development)

# S3 Configuration
S3_RECEIPTS_BUCKET=trackmyexpense-receipts-prod-741846356523

# Cognito Configuration
COGNITO_USER_POOL_ID=ap-south-1_Eq3rKzjGa
COGNITO_CLIENT_ID=i1rnn6it5gsf08744g172ajsa

# JWT Configuration
# Note: Cognito handles JWT tokens, but if you need custom JWT for other purposes
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-CHANGE-ME
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=30d

# Application Configuration
NODE_ENV=production
PORT=4000
LOG_LEVEL=info

# CORS Configuration
# Update with your actual frontend domain once deployed
ALLOWED_ORIGINS=http://localhost:5173,http://trackmyexpense-frontend-prod-741846356523.s3-website.ap-south-1.amazonaws.com
